#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
NODE_BIN="/Users/yangchunjiang/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node"
NODE_MODULES="/Users/yangchunjiang/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules"

NODE_PATH="$NODE_MODULES" "$NODE_BIN" "$ROOT_DIR/scripts/register-team-account.mjs" "$@"
